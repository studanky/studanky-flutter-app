name: studanky_flutter_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  flutter_svg: ^2.2.1
  go_router: ^14.1.4
  latlong2: ^0.9.1
  mobile_scanner: ^7.1.2
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.9
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - lib/core/assets/
